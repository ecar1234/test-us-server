export class Application {
    constructor(
        id: string | null,
        platform: string,
        status: string,
        appliedAt: Date | null = new Date(),
        updatedAt: Date | null = null
    ) { }
}